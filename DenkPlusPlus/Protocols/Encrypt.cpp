#include "Protocols.h"
#include <iostream>

using namespace Protocols;
using namespace std;

void EncryptClass::encrypt(const string& message, int iterations) {
    cout << "this function has been run" << message << iterations << endl;
}
